package eu.avalanche7.paradigm.configs.schema;

public enum ConfigFieldType {
    BOOLEAN,
    INTEGER,
    DOUBLE,
    STRING,
    ENUM,
    STRING_LIST,
    DURATION,
    COLOR,
    READ_ONLY_TEXT,
    SECRET_MASKED,
    PERMISSION_NODE,
    COMMAND_ID
}
